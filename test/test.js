const expect = require('expect');

const DividendToken = artifacts.require('./DividendToken.sol');

contract("DividendToken", () => {
  it('should have a test', () => {
    expect(true).toBe(false, "Need to write tests!");
  })
})